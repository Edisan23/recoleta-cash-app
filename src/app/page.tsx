'use client';

import { LandingPage } from '@/components/LandingPage';

export default function Home() {
  // Directly return the LandingPage, which now serves as the main entry point.
  return <LandingPage />;
}
