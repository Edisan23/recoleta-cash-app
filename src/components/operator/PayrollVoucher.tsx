'use client';
import React from 'react';

// This component is intentionally left blank as the history feature has been removed.
export const PayrollVoucher = React.forwardRef<HTMLDivElement, any>(({}, ref) => {
    return <div ref={ref} />;
});
PayrollVoucher.displayName = 'PayrollVoucher';
